import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.llamadev.app.indices',
  appName: 'Indices',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
